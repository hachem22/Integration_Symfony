{% extends 'stock_pharmacie/navbar.html.twig' %}

{% block title %}Modifier une Réclamation{% endblock %}

{% block body %}
   <!-- Navbar -->
                <nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme" id="layout-navbar">
                    <div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
                        <!-- Logo -->
                        <div class="navbar-nav align-items-center">
                            <div class="nav-item d-flex align-items-center">
                                <a href="{{ path('app_home') }}" class="app-brand-link">
                                    <img src="{{ asset('img/1.png') }}" alt="Logo" style="height: 40px;">
                                    <span class="app-brand-text demo menu-text fw-bolder ms-2">CareConnect</span>
                                </a>
                            </div>
                        </div>

                        <!-- Menu principal -->
                        <ul class="navbar-nav me-auto">
                            <!-- Rendez-vous -->
                            
                           <!-- Stock Pharmacie -->
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_test_dashboard') }}">
                                    <i class="bx bx-capsule me-1"></i>
                                    <span>Accueil</span>
                                </a>
                            </li>

                            <!-- Disponibilité des lits -->
                           

                            <!-- Événements -->
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                    <i class="bx bx-calendar-event me-1"></i>
                                    <span>Événements</span>
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item" href="#">
                                            <i class="bx bx-list-ul me-2"></i>
                                            Liste des événements
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="#">
                                            <i class="bx bx-plus-circle me-2"></i>
                                            Ajouter un événement
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="#">
                                            <i class="bx bx-calendar me-2"></i>
                                            Calendrier
                                        </a>
                                    </li>
                                </ul>
                            </li>

                             <!-- Réclamations -->
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                    <i class="bx bx-message-square-dots me-1"></i>
                                    <span>Réclamations</span>
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item" href="{{ path('reclamation_index1') }}">
                                            <i class="bx bx-list-ul me-2"></i>
                                            Liste des réclamations
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('reclamation_new1') }}">
                                            <i class="bx bx-plus-circle me-2"></i>
                                            Nouvelle réclamation
                                        </a>
                                    </li>
                                </ul>
                            </li>


                            <!-- Dashboard Patient -->
                           

                            
                            <li class="nav-item">
                             <a class="nav-link dropdown-toggle" href="{{ path('app_panier') }}">
                       <i class="bx bx-cart me-1"></i> Panier
                            </a>
                            </li>
                            
                        </ul>

                        <ul class="navbar-nav flex-row align-items-center ms-auto">
                            <!-- Search -->
                            <li class="nav-item navbar-search-wrapper me-2 me-xl-0">
                                <div class="nav-item nav-search d-none d-md-block">
                                    <i class="bx bx-search fs-4 lh-0"></i>
                                    <input type="text" class="form-control border-0 shadow-none" placeholder="Search..." aria-label="Search..." />
                                </div>
                            </li>

                            <!-- User -->
                            <li class="nav-item navbar-dropdown dropdown-user dropdown">
                                <a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);" data-bs-toggle="dropdown">
                                    <div class="avatar avatar-online">
                                        <img src="{{ asset('assets/img/avatars/1.png') }}" alt class="w-px-40 h-auto rounded-circle" />
                                    </div>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="#">
                                            <div class="d-flex">
                                                <div class="flex-shrink-0 me-3">
                                                    <div class="avatar avatar-online">
                                                        <img src="{{ asset('assets/img/avatars/1.png') }}" alt class="w-px-40 h-auto rounded-circle" />
                                                    </div>
                                                </div>
                                                <div class="flex-grow-1">
                                                    <span class="fw-semibold d-block">John Doe</span>
                                                    <small class="text-muted">Admin</small>
                                                </div>
                                            </div>
                                        </a>
                                    </li>
                                    <li><div class="dropdown-divider"></div></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-user me-2"></i>Mon Profil</a></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-cog me-2"></i>Paramètres</a></li>
                                    <li><div class="dropdown-divider"></div></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-power-off me-2"></i>Déconnexion</a></li>
                                </ul>
                            </li>
                        </ul>
                    </div>
                </nav>
                <!-- / Navbar -->
<div class="container-xxl flex-grow-1 container-p-y">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="col-xl" style="margin-left: 120px; width: 500px; margin-top: 200px;">
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            <i class="fas fa-edit"></i> Modifier la Réclamation
                        </h5>
                        <small class="text-body float-end">Mettez à jour les informations</small>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}

                        <!-- 📌 CHAMP DESCRIPTION -->
                        <div class="mb-3">
                            <label class="form-label">
                                <i class="bx bx-message-square-detail"></i> Description
                            </label>
                            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
                            {% if form.description.vars.errors|length > 0 %}
                                <span class="text-danger">{{ form_errors(form.description) }}</span>
                            {% endif %}
                        </div>

                        <!-- 📌 CHAMP TYPE -->
                        <div class="mb-3">
                            <label class="form-label">
                                <i class="bx bx-category"></i> Type de Réclamation
                            </label>
                            {{ form_widget(form.type, {'attr': {'class': 'form-select'}}) }}
                            {% if form.type.vars.errors|length > 0 %}
                                <span class="text-danger">{{ form_errors(form.type) }}</span>
                            {% endif %}
                        </div>

                        <!-- 📌 CHAMP CIBLE -->
                        <div class="mb-3">
                            <label class="form-label">
                                <i class="bx bx-user"></i> Cible
                            </label>
                            {{ form_widget(form.cible, {'attr': {'class': 'form-control'}}) }}
                            {% if form.cible.vars.errors|length > 0 %}
                                <span class="text-danger">{{ form_errors(form.cible) }}</span>
                            {% endif %}
                        </div>

                        <!-- 📌 CHAMP CATÉGORIE -->
                        <div class="mb-3">
                            <label class="form-label">
                                <i class="bx bx-list-check"></i> Catégorie
                            </label>
                            {{ form_widget(form.categorie, {'attr': {'class': 'form-select'}}) }}
                            {% if form.categorie.vars.errors|length > 0 %}
                                <span class="text-danger">{{ form_errors(form.categorie) }}</span>
                            {% endif %}
                        </div>

                        <!-- 📌 BOUTONS -->
                        <div class="card-footer d-flex justify-content-between align-items-center gap-2">
                            <a href="{{ path('reclamation_index1') }}" class="btn btn-secondary d-flex align-items-center gap-2">
                                <i class="fas fa-arrow-left"></i> Retour
                            </a>
                            <button type="submit" class="btn btn-primary d-flex align-items-center gap-2">
                                <i class="fas fa-save"></i> Mettre à jour
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
