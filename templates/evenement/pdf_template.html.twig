<html>
<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            font-size: 14px;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        h1 {
            text-align: center;
            font-size: 24px;
            color: #333;
            padding: 20px 0;
            background-color: #4CAF50;
            color: white;
            margin: 0;
        }
        p {
            font-size: 16px;
            color: #555;
            margin: 10px 0;
        }
        .container {
            width: 80%;
            margin: 0 auto;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        th {
            background-color: #f4f4f4;
            color: #333;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #f1f1f1;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ evenement.Nom }}</h1>
        <p><strong>Description:</strong> {{ evenement.Description }}</p>
        <p><strong>Capacité:</strong> {{ evenement.Capacite }}</p>
        <p><strong>Formation:</strong> {{ formationNom }}</p>
        <p><strong>Lieu:</strong> {{ evenement.lieu }}</p>
    </div>
</body>
</html>
